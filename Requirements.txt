1.Data manipulation and analysis:
	i)numpy==1.21.0
	ii)pandas==1.3.0

2.Data visualization:
	i)matplotlib==3.4.2
	ii)seaborn

3.Storage Service(Store & Retrieve Data)
	i)boto3
	ii)Amazon S3

4.Machine learning algorithms and utilities
	i)scikit-learn==0.24.2
	(# Includes StandardScaler for feature scaling)
	(# Includes train_test_split for data splitting)
	(# Includes LogisticRegression, RandomForestClassifier, DecisionTreeClassifier, SVC, and metrics)
	(# Includes GridSearchCV, RandomizedSearchCV, RandomForestClassifier)
	(# Includes roc_auc_score and roc_curve for model evaluation)
	(# Includes precision_recall_curve for model evaluation)
	
	ii)xgboost        
	(# Boosting library for gradient boosting)

	iii)scipy==1.7.0          
	(# Provides statistical functions for parameter distributions)

5.Deep learning library
	i)tensorflow==2.5.0  
	(# Includes Keras for defining neural network layers)

6.Web app framework for interactive data apps
	i)streamlit==0.82.0  
	(# For creating interactive web applications)

7.Model persistence
	i)joblib==1.1.0  
	(# For saving and loading machine learning models and objects)

8.Image processing library
	i)Pillow==8.3.1  
	(# For opening, manipulating, and saving image files)

